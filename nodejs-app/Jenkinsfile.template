pipeline {
   agent { label 'nodejs-app' }
  stages {
    stage('Test') {
      steps {
        container('nodejs') {
          echo 'Hello World!'   
          sh 'node -version'
        }
      }
    }
  }
  options { 
    buildDiscarder(logRotator(numToKeepStr: '2'))
  }
}
