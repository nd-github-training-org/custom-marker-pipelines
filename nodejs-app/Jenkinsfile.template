pipeline {
  agent any
  stages {
    stage('Say Hello') {
      steps {
        echo 'Hello World!'   
        sh 'java -version'
      }
    }
  }
  options { 
    buildDiscarder(logRotator(numToKeepStr: '2'))
  }
}
